id = "wincrt"
display_name = "CreateRemoteThread (official Windows crate)"
description = "Classic CreateRemoteThread via the official Windows crate. Easiest to spot but smallest footprint."
category = "injection"
tags = ["remote_injection"]
requires = ["target_process"]
incompatible_with = []
template_dir = "winCRT"

[[params]]
name = "target_process"
kind = "text"
label = "Target process"
default = "dllhost.exe"
