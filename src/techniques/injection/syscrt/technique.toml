id = "syscrt"
display_name = "NtCreateThreadEx via indirect syscalls"
description = "Remote NtCreateThreadEx via indirect syscalls (Dyncvoke) — bypasses user-mode EDR hooks on ntdll."
category = "injection"
tags = ["remote_injection", "syscall"]
requires = ["target_process"]
incompatible_with = []
template_dir = "sysCRT"

[[params]]
name = "target_process"
kind = "text"
label = "Target process"
default = "dllhost.exe"
