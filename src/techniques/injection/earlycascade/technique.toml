id = "earlycascade"
display_name = "EarlyCascade"
description = "EarlyCascade injection via shim engine callbacks."
category = "injection"
tags = ["remote_injection"]
requires = ["target_process"]
incompatible_with = []
template_dir = "ntEarlyCascade"

[[params]]
name = "target_process"
kind = "text"
label = "Target process"
default = "dllhost.exe"
